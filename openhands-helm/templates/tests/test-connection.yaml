apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openhands-helm.fullname" . }}-test-connection"
  labels:
    {{- include "openhands-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openhands-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
